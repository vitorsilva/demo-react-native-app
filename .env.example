# FTP credentials for landing page deployment (saborspin.com)
# Copy this file to .env and fill in your values
FTP_HOST=your-ftp-host.com
FTP_USER=your-username
FTP_PASSWORD=your-password

# ===========================================
# Telemetry Configuration
# ===========================================
# Telemetry settings are configured in app.json under expo.extra
# To enable telemetry, update app.json with:
#
#   "telemetryEnabled": true,
#   "telemetryEndpoint": "https://your-server.com/api/telemetry/ingest.php",
#   "telemetryToken": "your-secure-token",
#   "telemetryBatchSize": 10,
#   "telemetryFlushInterval": 30000
#
# Features:
# - Batching: Events queued and sent when batch size reached
# - Offline resilience: Events saved to AsyncStorage if network fails
# - Privacy: Sensitive data automatically redacted (passwords, tokens, emails, etc.)
# - No IP logging, no user identifiers, no location data
#
# For development, leave telemetryEnabled: false
